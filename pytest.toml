[pytest]
testpaths = ["tests"]
python_files = ["test_*.py"]
markers = [
  "python_scripts: unit tests for dev/support scripts under .scripts/",
  "agent: end-to-end tests that drive a real agent (slow, needs network + node)",
]
